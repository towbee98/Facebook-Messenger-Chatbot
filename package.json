{
    "name": "ecommerce-chatbot",
    "version": "1.0.0",
    "description": "an ecommerce bot using facebook messenger bot",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node dist/index.js",
        "build": "rm -rf dist && npm i && npx tsc -p tsconfig.json",
        "compile": "tsc && npm run start",
        "start:dev": "nodemon -e ts --exec \"npm run compile\""
    },
    "author": "Oladele Tobiloba",
    "license": "ISC",
    "devDependencies": {
        "@types/express": "^4.17.15",
        "dotenv": "^16.0.3",
        "typescript": "^4.9.4"
    },
    "dependencies": {
        "express": "^4.18.2",
        "nodemon": "^2.0.20",
        "prettier": "^2.8.1",
        "pug": "^3.0.2"
    }
}
